.DS_Store
Thumbs.db

# Persönliche Notizen — dieses Repo ist ÖFFENTLICH, und was einmal committet
# wurde, bleibt in der Historie lesbar, auch nach dem Löschen. Für versionierte
# Notizen ein eigenes PRIVATES Repo in diesen Ordner klonen (siehe Notizen/README).
Notizen/

# --- Android-Hülle: alles Erzeugte und alle Geheimnisse ---
# www/ und android/ entstehen aus der Repo-Wurzel (native/sync.mjs, cap add).
# Sie im Repo zu führen hieße, denselben Code zweimal zu pflegen — genau das,
# was diese Architektur vermeiden soll.
native/node_modules/
tools/node_modules/
tools/.ffprofile/
layout-messung.png
native/www/
native/android/
native/assets/
native/.env

# Signaturschlüssel gehören in ein Backup, nicht in ein öffentliches Repo.
*.keystore
*.jks
# Ohne Pfadangabe, also auf JEDER Ebene: Die Datei liegt in native/, der frühere
# Eintrag zeigte auf native/android/ — ein Verzeichnis, das ohnehin schon komplett
# ignoriert ist. Die echte Datei mit den Passwörtern war damit NICHT geschützt.
keystore.properties
TODO.md
